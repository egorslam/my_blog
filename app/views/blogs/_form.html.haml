= form_for @blog do |f|
  - if @blog.errors.any?
    #errors
      %h2= pluralize(@blog.errors.count, "error")
      %ul
        - @blog.errors.full_messages.each do |msg|
          %li= msg
  %p
    = f.label :title
    = f.text_field :title
  %p
    = f.submit
